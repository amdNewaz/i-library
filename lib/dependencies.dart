/*import 'package:flutter/foundation.dart';

GetIt dependency = GetIt.instance;

void init() {
  // Services
  dependency.registerLazySingleton(() => Rest());
 // dependency.registerLazySingleton<TodoService>(() => RentSettingScreen());
  //dependency.registerLazySingleton<UserService>(() => UserServiceRest());

  // Viewmodels
  dependency.registerLazySingleton(() => RentSettingScreen());
}*/
